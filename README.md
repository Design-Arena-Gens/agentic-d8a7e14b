# Media Usage Frequency Survey

Interactive survey matrix that lets audiences capture how often they interact with different news outlets, platforms, and apps. Built with Next.js and styled with modern CSS for an immersive, responsive experience.

## 🚀 Getting Started

### Prerequisites

- [Node.js](https://nodejs.org/) 18.18 or newer
- [npm](https://www.npmjs.com/) (bundled with Node.js)

### Installation

```bash
npm install
```

### Run the Development Server

```bash
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000) in your browser.

### Build for Production

```bash
npm run build
npm run start
```

## 📁 Project Structure

```
app/
  components/          # Survey UI
  globals.css          # Global styles
  layout.tsx           # Root layout & metadata
  page.tsx             # Landing page with survey
next.config.mjs        # Next.js configuration
package.json           # Scripts & dependencies
tsconfig.json          # TypeScript configuration
```

## 🧩 Features

- Matrix-style frequency selector with sticky activity column
- Local progress tracking with completion visualization
- Responsive layout and accessible HTML semantics
- Dark summary panel highlighting response distribution

## 🧪 Scripts

- `npm run dev` – start development server
- `npm run build` – create production build
- `npm run start` – serve the production build
- `npm run lint` – run Next.js lint rules

## 📄 License

MIT © Your Project Team
